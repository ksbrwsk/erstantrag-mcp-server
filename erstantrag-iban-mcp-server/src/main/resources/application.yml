spring:
  application:
    name: erstantrag-mcp-server
    type:

  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: erstantrag-iban-mcp-server
        version: 0.0.1
  threads:
    virtual:
      enabled: true
logging:
  file:
    name: ./erstantrag-iban-mcp-server.log
server:
  port: 8081
